[package]
name    = "reliquary"
version = "0.2.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name       = "Reliquary"

[dependencies]
stormlib-rs = { git = "https://github.com/notOrrytrout/stormlib-rs" }

[target.'cfg(windows)'.dependencies]
minhook = "0.9"
